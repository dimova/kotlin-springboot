# Project-wide Gradle settings.
# If the build fails with "25.0.2" or JavaVersion parse error (e.g. when using JDK 25),
# run Gradle with JDK 17: set JAVA_HOME to your JDK 17 installation and run again.

org.gradle.jvmargs=-Xmx512m -Dfile.encoding=UTF-8
org.gradle.parallel=true
